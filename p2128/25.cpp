#include<iostream>
#include<stdlib.h>
#include<time.h>

// Generate a random permutation of the elements of a list.
// Let's do this in-place with a pointer using the old shuffle algorithm
template<typename T, size_t N>
void rnd_permu(T (&v)[N]) {
    
}